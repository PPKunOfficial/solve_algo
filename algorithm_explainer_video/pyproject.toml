[project]
name = "algorithm-explainer-video"
version = "0.1.0"
description = "Manim algorithm explainer videos"
requires-python = ">=3.11,<3.14"
dependencies = [
    "manim>=0.19.0,<0.20",
]

[tool.uv]
package = false
